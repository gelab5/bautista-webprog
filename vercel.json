{
  "buildCommand": "cd bautista-client && npm install && npm run build",
  "outputDirectory": "bautista-client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}